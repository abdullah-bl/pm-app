---
/**
 * A card component for displaying grouped detail information
 * with a header area and content slots
 */
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["border border-border rounded-lg p-6", className]}>
	<slot />
</div>
